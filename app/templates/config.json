{
  <% if (projectProxy != '') { %>
  "proxy": "<%= projectProxy %>",
  <% } %>
  "projectType": "<%= projectUsage %>",

  "src": {
    
  },

  "dist": {
    <% i (projectUsage == 'Just Prototyping') { %>

    <% } %>

    <% i (projectUsage == 'Use with WordPress') { %>

    <% } %>

    <% i (projectUsage == 'Use with Craft CMS') { %>

    <% } %>

  }
}