var debug = true;

// Site Settings
var defaults = {
  breakpoints: {
    start: 0,
    small: 667,
    medium: 1024,
    large: 1280
  },
  timings: {
    zero: 0,
    fast: 250,
    medium: 500,
    slow: 1000,
  }
};

var _dh_options   = jQuery.extend(defaults, _dh_options);

/**
 * Test
 */