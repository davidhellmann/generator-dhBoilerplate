/**
 * Waypoints
 */

;(function($) {

  /*  var $_tabs = $('#products');

   $_tabs.waypoint(function(direction) {
   if (direction === 'down') {
   $(this.element).find('.m_sliderTabs').addClass('sticky');
   }
   }, {
   offset: '50'
   });

   $_tabs.waypoint(function(direction) {
   if (direction === 'up') {
   $(this.element).find('.m_sliderTabs').removeClass('sticky');
   }
   }, {
   offset: '0%'
   });

   $('#map_wrapper').waypoint(function(direction) {
   if (direction === 'down') {
   $('.m_sliderTabs').addClass("beamMeUp");
   setTimeout(function(){
   $('.m_sliderTabs').removeClass("beamMeUp sticky");
   }, 500);
   }
   }, {
   offset: '140'
   });*/

}(jQuery));
